# TODO

## Structure decision: Option B (parts/part-*/chapters/*)

- [ ] Inspect current `_quarto.yml` chapter path mapping (already gathered)
- [ ] Create new directory layout under `parts/part-1-getting-comfortable/chapters/` etc.
- [ ] Move existing `chapters/chapter*.qmd` into the correct part directory (matching current Part I–IV grouping)
- [ ] Update `_quarto.yml` to point to the new chapter paths
- [ ] Update any internal links/relative image/code references if present
- [x] Run `quarto render` (HTML) to confirm the book builds

